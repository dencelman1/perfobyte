
// custom imports:

import {default as v, value as anoth} from './value.js';
import v from './value.js';

export {default as v} from './value.js';

export var
    another_value = 10,
    processed = (v + anoth)
;
export default (
    (global) => {
        return 0;
    }
);
